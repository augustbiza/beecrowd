// 1007 - Diferença
#include <iostream>

int main(void) {

    int a, b, c, d, diferenca;
    std::cin>>a>>b>>c>>d;

    diferenca = (a*b - c*d);

    std::cout<<"DIFERENCA = "<<diferenca<<std::endl;
}